# New-lab (experimental track)

**Status:** Reorganized Day 1 labs (ROS readiness → pub/sub → DDS → sport → RViz). **Not fully verified** on hardware; kept here while `course/day-01/` at the repo root matches **main** for merge.

Run scripts from paths under `course/day-01/New-lab/` (see each lab README).

Shared helpers on **main** layout: [`../go2_motion_helpers.py`](../go2_motion_helpers.py).
